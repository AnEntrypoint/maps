{
  "project": "cs_16_maps_conversion",
  "created": "2026-02-24",
  "objective": "Download Counter-Strike 1.6 maps from GitHub and convert all BSP files to GLB format with embedded textures",
  "items": [
    {
      "id": "3",
      "subject": "Inventory all BSP files and detect WAD dependencies",
      "status": "pending",
      "description": "Scan the cloned repository to find all .bsp files. Parse each BSP to identify required WAD texture files. Create mapping of maps to their texture dependencies.",
      "blocking": ["4"],
      "blockedBy": [],
      "effort": "medium",
      "category": "analysis",
      "edge_cases": [
        "Maps with embedded textures only",
        "Maps requiring external WADs",
        "Maps with missing WAD files",
        "Maps with standard/custom texture sets"
      ],
      "acceptance": [
        "List of all BSP maps generated",
        "WAD dependencies identified for each map",
        "Report includes texture sources (embedded vs external)"
      ]
    },
    {
      "id": "4",
      "subject": "Convert all BSP maps to GLB with textures",
      "status": "pending",
      "description": "For each BSP file found, run bsp-converter with texture extraction enabled (-tex flag). Convert each map to GLB format. Ensure textures are embedded in output files.",
      "blocking": ["5"],
      "blockedBy": ["3"],
      "effort": "large",
      "category": "conversion",
      "notes": "Use -tex flag to include all textures, -skip_sky to remove sky polygons for cleaner output, -lm 2048 for lightmap size",
      "acceptance": [
        "All BSP files converted to GLB",
        "Textures embedded in each GLB file",
        "Conversion completes without errors",
        "Output files organized by map name"
      ]
    },
    {
      "id": "5",
      "subject": "Verify GLB output files and texture integrity",
      "status": "pending",
      "description": "Inspect all generated GLB files. Verify they contain proper geometry, materials, and textures. Check file sizes are reasonable. Generate validation report.",
      "blocking": [],
      "blockedBy": ["4"],
      "effort": "medium",
      "category": "verification",
      "edge_cases": [
        "Corrupted GLB files",
        "Missing material definitions",
        "Texture format issues",
        "Lightmap generation failures"
      ],
      "acceptance": [
        "All GLB files are valid and readable",
        "Material count > 0 for each file",
        "Texture count > 0 for each file",
        "File sizes within expected range",
        "Validation report generated"
      ]
    }
  ],
  "completed": [
    {
      "id": "1",
      "subject": "Build bsp-converter if not already compiled",
      "status": "completed"
    },
    {
      "id": "2",
      "subject": "Clone cs_16_maps repository from GitHub",
      "status": "completed",
      "note": "Repository contains RMF source files (not compiled BSP). Awaiting user decision on compilation approach."
    }
  ]
}
